<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#15315a"/>
      <stop offset="100%" stop-color="#091526"/>
    </linearGradient>
  </defs>
  <rect width="120" height="120" rx="26" fill="url(#bg)" />
  
  <!-- Vertical Pill (Digital/Teal) -->
  <rect x="50" y="28" width="20" height="64" rx="10" fill="#329999" opacity="0.9" />
  
  <!-- Horizontal Pill (Academic/Gold) -->
  <rect x="28" y="50" width="64" height="20" rx="10" fill="#c47c3e" opacity="0.95" />
  
  <!-- Central Hub (Node) -->
  <circle cx="60" cy="60" r="5" fill="#ffffff" />
</svg>
